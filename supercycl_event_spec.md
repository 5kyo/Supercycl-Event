# Supercycl Mobile Launch Festival — 개발 명세서

> **TRADE DIFFERENT · RIDE THE SUPERCYCL**
> Supercycl 모바일 PWA 정식 출시 기념 1개월 한정 캠페인

---

## 0. 캠페인 기본 정보

| 항목 | 내용 |
|------|------|
| **명칭** | Supercycl Mobile Launch Festival |
| **기간** | 2026-06-08 (월) 00:00 ~ 2026-07-07 (화) 23:59 KST |
| **대상** | **YouthMeta 멤버 한정** (신규 + 기존 모두 · 본 서비스 YouthMeta 로스터 등록자) · **OKX 거래소 연동 완료자 한정** · 비유스메타 계정 참여 불가 |
| **이벤트 페이지** | **별도 외부 웹사이트** (Supercycl 모바일/PC 본 서비스와 분리) |
| **연계 채널** | Supercycl 모바일 PWA (출시 기념 대상), 거래소 OAuth, 마이페이지(본 서비스) |
| **총 보상 예산** | 100,000 ICX + 10,000 USDT |
| **운영 방식** | 2단계 Sequential (거래 W1~W3, 6/8~6/28 → 설문 W4, 6/29~7/5) |

### 0.0 본 명세서 범위

**포함 범위 (In Scope)**
- 캠페인 운영 정책 (기간, 보상, 자격 조건, 어뷰징 방지)
- 사용자 여정 및 UI 명세 (이벤트 페이지)
- 인앱 알림 정책 / 검증 트랙
- 개발 우선순위 및 Open Issues

**범위 외 (Out of Scope) — 별도 문서로 작성**
- **API 엔드포인트 / DB 스키마 / 인증 토큰 상세** → 개발팀이 별도 기술 명세서로 작성
- **거래소 OAuth / API 키 등록 / 거래량 집계 로직** → 본 서비스 기존 구현 그대로 사용 (재정의 없음)
- **이벤트 약관 / 개인정보 처리방침** → **별도 이벤트 약관을 두지 않음.** 본 서비스 가입 시점에 동의한 가입 약관·개인정보 처리방침이 본 이벤트 참여를 포함하는 것으로 운영(이벤트 페이지 내 별도 동의 절차 없음). 슬롯 박탈/연장/조기 종료/어뷰징 보류 등 운영 재량 항목은 **운영팀 내부 운영 매뉴얼**로 관리
- **운영자 어드민 시스템** (지급 대기 큐, 자격 충족자 + 연동된 OKX UID export, 지급 완료 토글, TX 해시 입력, 보류 처리, 히스토리 로그, 권한 분리 등) → 본 명세 범위 외. 운영팀이 별도 도구(전용 어드민 구축 / Notion+CSV 수동 운영 / 본 서비스 어드민 통합 등)로 자체 운영. 본 명세서는 **운영 결과가 이벤트 페이지에 반영되는 인터페이스**(보상 상태 라벨 갱신 등)만 정의

### 0.1 아키텍처 개요 (이벤트 페이지 분리) ⚠️

```
┌─────────────────────────────────┐      ┌─────────────────────────────────┐
│   이벤트 페이지 (외부 링크)         │      │   Supercycl 본 서비스             │
│   예: event.supercycl.io         │◀────▶│   (Mobile PWA / PC Web)         │
│                                 │      │                                 │
│   - 랜딩 (공개/로그인 분기)         │      │   - 가입/KYC                     │
│   - 이벤트 허브 (진척도/슬롯/보상) │      │   - 거래소 OAuth 연결              │
│   - 설문 12문항                   │      │   - 실제 거래 실행                 │
│   - 보상 상태 표시                 │      │   - 마이페이지                    │
│                                 │      │                                 │
└─────────────────────────────────┘      └─────────────────────────────────┘
                ▲                                          ▲
                │                                          │
                └──────────── 공통 인증 (SSO/Token) ─────────┘
                        + 공통 백엔드 API
                        + 공통 유저 DB
```

**핵심 원칙**
- 이벤트 페이지는 **독립 도메인/서브도메인**으로 분리 (예: `event.supercycl.io`)
- 본 서비스와 **유저 인증을 공유** (SSO 또는 토큰 기반) — 별도 회원가입 없음
- 거래/KYC 등 **본 서비스 기능 호출만 deeplink/redirect**로 처리
- **보상 지급은 OKX Internal Transfer** — 로그인 시 연동된 OKX UID로 운영자가 직접 송금 (이벤트 페이지 내 별도 수령 정보 등록 단계 없음)
- 이벤트 종료 후 페이지는 **archive 모드**로 전환 가능 (별도 배포로 본 서비스 무관)
- 모든 데이터는 **동일 백엔드/DB** 사용 (분석/정산 일원화)

---


## 1. 캠페인 구조 (Sequential Funnel)

자연스러운 onboarding funnel로 두 이벤트를 단순 병행이 아닌 순차 운영.

### STEP 1 — Sign Up (전 기간)
- **YouthMeta 멤버 확인** (본 서비스 로스터 등록 여부)
- YouthMeta 멤버 로그인 또는 신규 가입
- **OKX 거래소 연동 완료** (이번 캠페인 한정 — OKX 사용자만 참여 가능)
- → **단일 entry로 통합** (STEP 1 done 조건 = 본 서비스 로그인 AND YouthMeta 멤버 AND OKX 연동)

> 📌 **YouthMeta 자격 정책**: 이번 캠페인은 **YouthMeta 멤버 전용**입니다. "YouthMeta 멤버"의 정의는 **본 서비스의 YouthMeta 로스터에 등록된 계정**(신규/기존 무관). 본 서비스 로그인 직후 백엔드가 YouthMeta 멤버십을 확인해 이벤트 페이지에 전달하며, 비유스메타 계정은 거래/설문 흐름에 진입할 수 없고 차단 화면(§7.3.1)으로 라우팅됩니다.
>
> **Why 차단을 진입 시점에 두는가**: 자격 미충족 유저가 거래/설문까지 진행할 경우 운영자 정산 단계에서 일괄 박탈해야 하는 부담이 발생합니다. 진입 시점 차단이 운영 비용·유저 혼란 모두에서 유리합니다.

> 📌 **OKX 자격 정책**: 이번 캠페인의 거래소 조건은 **OKX 거래소 연동 완료** 기준입니다. KYC 미완료 유저는 자체적으로 OKX 연동이 불가하므로, 본 서비스 정책상 OKX 연동 완료 = KYC 완료 + 거래소 OAuth/API 키 등록 완료 상태로 간주합니다.

### STEP 2 — Trade & Win (1~3주차, 6/8 ~ 6/28)
- 누적 거래량 $500 충족
- **선착순 500명 × 20 USDT**
- 슬롯 카운터 실시간 노출(FOMO)
- 거래 이벤트는 1~3주차에만 단독 운영

### STEP 3 — Profile & Earn (4주차, 6/29 ~ 7/5)
- 12문항 설문 응답
- **ICX 지급** (거래자 100 ICX / 비거래자 ⏳추후 확정, §3 참조)
- 설문은 4주차에만 단독 운영 (거래 경험 후 informed feedback)

---

## 2. KPI

| 트랙 | 핵심 지표 | 타겟 | 의미 |
|------|----------|------|------|
| **LEAD** | 설문 완료자 수 | 700명 | 거래자 풀 100 ICX × 700명 = 70,000 ICX 한도 (별도 비거래자 풀 30,000 ICX 추가 존재) |
| **REVENUE** | 거래량 $500+ 달성자 수 | 500명 | 선착순, 자동 마감 |
| **보조** | 1인당 평균 거래량 | $1,500+ | $500 도달 후 거래 지속 여부 |
| **VALIDATION** | 2x 레버리지 캡 이탈률 | 측정 자체가 목표 | 첫 데이터 포인트 확보 |

---

## 3. 예산 정책

### 설문 보상 — 총 100,000 ICX

설문 응답자는 **거래 여부에 따라 차등 지급**합니다.

| 응답자 구분 | 인당 지급량 | 풀 |
|------------|------------|----|
| **거래자** (캠페인 기간 중 1회 이상 거래 체결) | **100 ICX** | 70,000 ICX (최대 700명) |
| **비거래자** (캠페인 기간 중 거래 0회) | **추후 확정** ⏳ | 30,000 ICX (인원 미정) |

- 거래자의 인당 지급량과 풀 크기는 §4.2와 정합
- 비거래자의 인당 지급량은 운영팀 의사결정 후 확정 (§12 Open Issues 참조)
- "거래자" 정의 = **캠페인 기간 내 Supercycl 통해 1회 이상 체결한 유저** (자전거래 제외)

### 거래 보상 — 총 10,000 USDT
- 500명 × 20 USDT
- 정산: 매주 월요일 10:00 KST
- 지급 채널: 로그인 시 연동된 OKX UID로 **OKX Internal Transfer** (별도 수령 정보 등록 단계 없음 — 참여 자격 자체가 OKX 연동 완료 기준)

### 운영 원칙
- 시세 변동과 무관하게 **고정 수량 지급**
- 슬롯/예산 한도 도달 시 **자동 마감**
- 슬롯 박탈/연장/조기 종료 권한은 **운영팀 운영 매뉴얼**에 명시 (별도 이벤트 약관 없음)

---

## 4. 이벤트 1 — 설문 (Profile & Earn)

### 4.1 참여 조건
- **YouthMeta 멤버 한정** (본 서비스 YouthMeta 로스터 등록자 · 신규/기존 무관). 비유스메타 계정은 이벤트 진입 시점에 차단됩니다 (§7.3.1)
- 이벤트 기간(1개월) 내 1회 응답
- 모든 필수 문항 응답 시 완료 처리
- **YouthMeta 멤버 ID 1건당 1회만 응답** (서버측 검증)

### 4.2 보상 정책
| 항목 | 내용 |
|------|------|
| 보상 | **거래자 100 ICX / 비거래자 ⏳추후 확정** (§3 풀 분리 참조) |
| 지급 시점 | 응답 완료 후 7영업일 이내 |
| 지급 방식 | **운영팀 수동 송금** — 로그인 시 연동된 OKX UID로 OKX Internal Transfer (유저 액션 불필요) |
| 클레임 버튼 | **없음** (자동 지급, 유저 액션 불필요) |
| 검증 데이터 | 거래자/비거래자별 응답 완료율 |
| 타겟 응답 수 | 700명 (거래자 풀 기준) + 비거래자 응답자 |
| 예상 응답 시간 | 7~9분 |
| 예산 | 70,000 ICX (거래자 풀) + 30,000 ICX (비거래자 풀) = **100,000 ICX 총** |

> 📌 **ICX 지급도 USDT와 동일하게 OKX Internal Transfer로 일원화합니다.** 캠페인 참여 자격이 OKX 연동 완료이므로, 별도 수령 주소를 받지 않고 시스템이 이미 알고 있는 OKX UID로 운영자가 송금합니다.

#### 설문 완료 직후 미니 리포트 (모달, §9.1 #2 트리거)

코드: `src/components/modals/SurveyCompleteModal.tsx`. 응답 완료 직후 1회 표시(`dismissedFlags.surveyCompleteSeen`로 dismiss 저장).

| 블록 | 표시 항목 | 소스 |
|---|---|---|
| **Time spent** | 응답 총 소요시간 (mm:ss) + 평균 응답시간 per question (Avg sec/Q) | 클라이언트 timestamp 측정 |
| **Your profile** | Trader 여부 / Top venues / Avg leverage / Interest | 설문 응답 기반 요약 (현재 코드는 mock 고정값 — 실제 응답에서 추출 필요) |
| **CTA** | Done 버튼만 노출 (별도 입력 단계 없음) | 모달 dismiss와 함께 "연동된 OKX UID로 7영업일 내 자동 송금" 안내

### 4.3 설문 구성 (12문항)

> 응답은 **이벤트 페이지 내 설문 폼**에서 진행됩니다 (본 서비스 이동 없음).
> 문항 원본은 `src/content/survey-ko.md`이며, 본 표는 운영 정책 참조용입니다 (실제 문항 텍스트는 마크다운 원본을 단일 source of truth로 사용).

| # | 영역 | 문항 | 형식 |
|---|------|------|------|
| 1 | 만족 | Supercycl을 쓰면서 가장 좋았던 점 | 다중 선택 (속도/단순흐름/디자인/시그널/차트/거래소통합/수수료/리워드/기타) |
| 2 | UX | 가장 조작이 어려웠던 화면/기능 | 단일 + 자유 |
| 3 | UX | 추가되었으면 하는 기능 TOP 1 | 자유 (선택) |
| 4 | 가치제안 | Supercycl을 쓴다면 가장 매력적인 기능 | 단일 (시그널/차트/거래소통합/수수료/**보안**/리워드) |
| 5 | 자동매매 | AI 자동매매 출시 시 사용 의향 | 5점 척도 |
| 6 | 거래소 | 현재 가장 많이 쓰는 거래소 3곳 | 다중 (Bybit/OKX/Bitget/Gate/Binance/기타) |
| 7 ⭐ | 2x 레버리지 | 다른 거래소 평균 사용 레버리지 | 단일 (1x/2x/3x/5x/10x+/거래 안 함) |
| 8 ⭐ | 2x 레버리지 | 2x 제한이 Supercycl 사용에 영향을 미치나요 | 5점 척도 |
| 9 ⭐ | 2x 레버리지 | 2x 제한 있어도 쓸 의향이 있다면 그 이유 | 다중 |
| 10 | 인지 | Supercycl을 어떻게 알게 되었나요 | 단일 |
| 11 | 프로필 | 월평균 크립토 거래 금액대 (익명 보장) | 단일 (구간) |
| 12 | 시그널 개인화 | 관심 자산 카테고리 | 다중 (BTC/ETH/Alt/Meme/Index) |

> 📌 **이전 13문항 구성에서 변경된 점** (커밋 `48609f2`):
> - **Q1 재설계**: "PWA 사용 시 가장 만족스러운 점" → "Supercycl을 쓰면서 가장 좋았던 점" (전반 만족도)
> - **Q4 통합**: 신뢰 영역(애그리게이터 선택 시 중요한 것) 문항을 별도로 두지 않고 Q4 옵션에 `보안`을 흡수해 통합
> - **신뢰 영역 단독 문항 제거**: Q4에서 신호로 측정 (별도 단일 문항 불요)
> - **번호 시프트**: 기존 Q8~Q13 → Q7~Q12

### 4.4 어뷰징 방지 (3 레이어)

| 레이어 | 방식 | 정책 |
|--------|------|------|
| **L1 — ID** | YouthMeta Member ID & Supercycl Account | 1건당 1회만 응답 가능, 서버측 검증 |
| **L2 — Device** | 디바이스 fingerprint | 동일 fingerprint 5회 이상 시도 → 일시 차단 |
| **L3 — 응답시간** | 응답 소요 시간 측정 | 비정상적으로 짧은 응답 필터링 |

> 📌 **L2/L3 구현 판단**: 위 정책은 운영 목표(목표 상태)를 기술한 것입니다. **L2(디바이스 fingerprint)와 L3(응답시간)의 실제 구현 여부·수단·임계값은 개발팀 자체 판단에 일임**합니다 (라이브러리 도입 부담, 백엔드 저장 비용, 어뷰징 위험도 ROI를 함께 검토). L1(1인 1회 서버측 검증)은 필수 구현 항목입니다.

---

## 5. 이벤트 2 — 거래 활성화 (Trade & Win)

### 5.1 참여 조건 & 보상

| 항목 | 내용 |
|------|------|
| 참여 조건 | **YouthMeta 멤버 한정** (본 서비스 로스터 등록자) + **OKX 거래소 연동 완료** + 1개월 내 누적 거래량 $500 USD 이상. 비유스메타 계정은 이벤트 진입 시점에 차단 (§7.3.1) |
| 보상 | 공통 20 USDT |
| 한도 | 선착순 500명 (예산 도달 시 자동 마감) |
| 정산 주기 | 매주 월 10:00 KST (전주 일요일 23:59 KST 기준 자격 충족자) |
| 지급 방식 | **운영팀 수동 송금** (시스템은 자격 충족자 리스트만 산출, 실제 송금은 운영자가 수행) |
| 클레임 버튼 | **없음** (자동 지급, 유저 액션 불필요) |
| 지급 채널 | 로그인 시 연동된 OKX UID로 **OKX Internal Transfer** (이벤트 페이지에서 별도 수령 정보 입력 단계 없음) |

### 5.2 "거래량" 정의

**✓ 포함**
- 체결된 선물 포지션의 명목 가치(notional value) 합계
- 매수 + 매도 모두 합산 (oneway/hedge 무관)
- Supercycl 통해 체결된 거래만 (브로커 ID로 필터링)

**✗ 미포함**
- 미체결 주문, 취소 주문
- 입금 / 출금
- 직접 거래소에서 체결한 거래

> **예시:** $250 명목 BTC 롱 + $250 ETH 숏 = $500 도달 → 자격 획득

### 5.3 슬롯 카운터 (FOMO UI)

- 이벤트 허브 페이지 보상 영역 상단에 상시 노출
- 표시 형식: `"잔여 357 / 500 슬롯"`
- **5분마다 갱신**
- 100슬롯 단위 도달 시 **이벤트 페이지 상단 배너로 인앱 안내** (다음 페이지 방문 시 표시)
  - 예: "이벤트 잔여 슬롯 100개! 지금 거래 시작하세요"

### 5.4 사용자 진척도 UI

```
누적 거래량  $237 / $500
📊 남은 거래량  $263
🏁 잔여 357명
⏰ 종료까지 18일
[지금 거래하기 →]
```

**전환 압력 3축:** 진행률 + 잔여 슬롯 + 종료까지 시간

### 5.5 자격 검증 & 정산 플로우

#### 자격 도달 처리 (백엔드 자동)
```
1. 브로커 API로 거래 데이터 수집·집계 (방식: 배치/실시간/이벤트 기반 등 백엔드 구현에 일임)
        ↓
2. 유저별 누적 거래량 업데이트 (DB)
        ↓
3. 신규 도달자 식별 (≥ $500)
        ↓
4. 선착순 슬롯 할당 (도달 시각 기준 정렬)
        ↓
5. 당첨자 인앱 상태 갱신 (다음 페이지 방문 시 "슬롯 확보" 모달 + 보상 영역 `AWAITING_PAYOUT` 라벨 표시)
```

> 📌 **자격 도달 처리 방식**: spec은 결과(자격 충족자 식별·슬롯 할당)만 정의합니다. **배치 주기·실시간 처리·이벤트 기반 등 구체 방식은 백엔드 구현에 일임**합니다. 단 매주 일요일 23:59 KST까지의 거래는 다음 정산일(월요일 10:00 KST) 전에 모두 반영되어야 합니다 (정산 정합성 요구사항).

#### 슬롯 마감 후 도달자 처리

500명 슬롯 소진 후 추가로 $500 도달한 유저가 발생할 경우:

| 항목 | 정책 |
|------|------|
| 보상 | 없음 (예산 한도 도달, 추가 지급 불가) |
| 안내 시점 | 도달 즉시 (자격 도달 처리 사이클 완료 후 다음 페이지 방문 시) |
| 안내 표시 | 이벤트 페이지 보상 영역 — USDT 영역에 안내 텍스트 표시 |
| 안내 텍스트 | **"$500 거래 달성을 축하드립니다. 다만 슬롯이 모두 마감되어 보상 지급은 불가합니다. 참여해주셔서 감사합니다."** |
| 슬롯 확장 시 | 운영팀이 슬롯 확장(+200) 결정 시(§12 Open Issues) 도달 시각 순으로 자동 슬롯 할당 |

#### 주간 정산 — 매주 월요일 10:00 KST
1. **시스템**: 전주 일요일 23:59 KST 기준 자격 충족자 리스트 생성 → 운영자가 확인할 수 있는 형태로 제공 (어드민 시스템은 본 명세 범위 외 — §0.0 참조)
2. **운영자**: 대상자 리스트 확인 (의심 케이스 7일 보류 가능)
3. **운영자**: 각 유저에게 USDT 수동 송금
4. **운영자**: 송금 완료된 유저별로 **"지급 완료" 상태 마킹** (운영자 도구 → 백엔드 API)
5. **시스템**: 상태 변경 시 이벤트 페이지 보상 상태 라벨 즉시 반영 (별도 외부 알림 발송 없음, 유저는 페이지 진입 시 상태 확인)

> 의심 케이스는 7일 보류 후 수동 판단 (운영팀 운영 매뉴얼)

### 5.6 어뷰징 방지 (이벤트 1보다 강화)

| 검증 항목 | 정책 |
|----------|------|
| YouthMeta KYC | 멤버십 ID 기준 1인 1슬롯 |
| 자전거래 (Wash Trading) | 동일 가격대 즉시 반대 포지션 → 거래량 미인정 |
| 짧은 보유 시간 | **이번 캠페인 미시행** — "평균 보유 시간 < 60초 거래량 50% 차감" 정책은 차기 캠페인에서 시행 여부 재논의 (구현 부담 vs 효과 트레이드오프) |
| 디바이스/IP 중복 | 동일 fingerprint 또는 IP 다중 계정 → 전수 검토 |
| 거래소 UID 중복 | 동일 OKX UID 1회만 슬롯 점유 (OAuth 연동 시점에 중복 검출, 별도 사용자 입력 단계 없음) |
| 정산 보류 권한 | 운영팀이 의심 케이스 7일간 보류 후 수동 판단 (운영팀 운영 매뉴얼) |

### 5.7 USDT 지급 채널 (OKX Internal Transfer)

거래 자격($500 도달) 충족 시 **별도 수령 정보 입력 단계 없이** 시스템이 이미 알고 있는 OKX UID로 운영자가 OKX Internal Transfer를 실행합니다.

- 참여 자격이 **OKX 연동 완료**(STEP 1)이므로 OAuth 완료 시점에 OKX UID가 시스템에 저장되어 있습니다.
- 이벤트 페이지에는 유저가 직접 주소를 입력하는 모달/폼이 존재하지 않습니다 (보상 영역은 read-only 상태 표시 + 연동된 마스킹 UID 표시).
- 운영자는 자격 충족자 리스트를 받아 각 유저의 연동 OKX UID로 Internal Transfer 실행 → "지급 완료" 마킹.
- 약관 동의는 **본 서비스 가입 시점**의 가입 약관·개인정보 처리방침으로 갈음합니다 (별도 이벤트 약관 없음).

> 📌 **다른 네트워크/거래소 지원 안 함.** 이번 캠페인 참여 자격이 OKX 한정이므로, 송금도 OKX Internal Transfer로 일원화합니다. 외부 지갑 송금은 자산 손실 위험·운영 비용·재차 입력 마찰을 모두 추가하므로 사용하지 않습니다.

---

## 6. 사용자 여정 (Unified Journey)

| 시점 | 단계 | 액션 | 위치 |
|------|------|------|------|
| **6/8 (D-day)** | 캠페인 인지 | YouthMeta 멤버십 영역 / 공개 랜딩 URL → **이벤트 랜딩 진입** | 외부 이벤트 페이지 |
| **6/8 ~ 6/9** | 가입 또는 로그인 | 이벤트 페이지에서 "참여하기" → 본 서비스로 redirect → 가입/로그인 → KYC → 거래소 OAuth → 이벤트 페이지로 복귀. 로그인 직후 백엔드가 YouthMeta 멤버십을 확인 → **비유스메타 계정은 이벤트 페이지가 YouthMeta-only 차단 화면을 렌더링** (Hero 유지 + 안내 + YouthMeta 가입 link-out + Sign out, §7.3.1) | 외부 → 본 서비스 → 외부 |
| **6/8 ~ 6/28** | 거래 도달 ($500 → 20 USDT) | 이벤트 허브에서 진척도/슬롯 확인 → "거래하러 가기" 클릭 → 본 서비스 PWA로 deeplink → 거래 실행 → 자격 충족 시 보상 영역 상태가 `AWAITING_PAYOUT`으로 전환 (수령 정보 입력 단계 없음 — 연동된 OKX UID로 자동 송금) | 외부 → 본 서비스 → 외부 |
| **6/29 ~ 7/5** | 설문 완료 (ICX 지급) | 이벤트 페이지에서 12문항 응답 → 즉시 미니 리포트 (수령 정보 입력 단계 없음 — 연동된 OKX UID로 자동 송금) | 외부 이벤트 페이지 |
| **7/7 (종료 직후)** | NPS + Exit 인터뷰 | 완료자: 인앱 모달 NPS 1문항 / 미완료자: 50명 표본 1:1 인터뷰 (별도 컨택) | 인앱 모달 / 별도 폼 |

### 6.1 페이지 간 이동 매트릭스

| From | To | 트리거 | 처리 방식 |
|------|----|----|----------|
| 이벤트 페이지 | 본 서비스 (가입/로그인) | "참여하기" / "Sign up / Log in to join" CTA | **디바이스 판별 redirect** — 모바일은 Supercycl Mobile PWA 로그인/가입 페이지, PC는 Supercycl PC 로그인/가입 페이지 + return URL 파라미터 (이벤트 페이지 내부 로그인 UI 없음) |
| 이벤트 페이지 | 본 서비스 (거래) | "거래하러 가기" CTA | deeplink (`supercycl://trade` 또는 PWA URL) |
| 이벤트 페이지 | **YouthMeta 가입 페이지(외부 사이트)** | 공개 랜딩의 "Learn how to join" 링크 / 비유스메타 차단 화면의 "Become a YouthMeta member" CTA | 외부 link-out (`target="_blank"`). URL은 환경변수 `NEXT_PUBLIC_YOUTHMETA_JOIN_URL`로 주입 (미주입 시 fallback 도메인). 이벤트 페이지 내부 가입 폼은 없음 |
| 본 서비스 | 이벤트 페이지 | KYC/OAuth 완료 후 자동 복귀 | return URL로 redirect (가입/연결 직후 1회) |

> 📌 **수령 정보 입력 단계 없음** — 보상은 모두 연동된 OKX UID로 운영자가 OKX Internal Transfer로 송금합니다. 유저는 거래 → 본 서비스 / 상태 확인 → 이벤트 페이지로 흐름이 분리됩니다.

> 📌 **본 서비스에서 이벤트 페이지로의 능동적 복귀 동선은 제공하지 않습니다.** 가입/OAuth 완료 직후 자동 redirect를 제외하고, 본 서비스 PWA 내부에는 이벤트 페이지로 돌아가는 별도 링크/버튼이 없습니다. 유저는 브라우저 뒤로가기 또는 탭 전환으로 이벤트 페이지에 복귀합니다.

---

## 7. 이벤트 허브 페이지 (UI 명세 — 외부 페이지)

> ⚠️ 이벤트 페이지는 **외부 별도 도메인**에 배포됩니다. 본 서비스(Supercycl Mobile PWA/PC)와 분리되어 있으며, 인증과 데이터는 공유 API로 동기화됩니다.

### 7.1 URL 정책 (단일 URL · 콘텐츠 분기)

- **단일 URL** — `event.supercycl.io` (또는 확정된 도메인) 하나로 운영
- **로그인 여부에 따라 같은 URL에서 다른 콘텐츠를 렌더링** (별도 페이지/별도 URL 없음)
- 비로그인 유저 → 공개 랜딩 콘텐츠
- 로그인 유저 → 개인화된 이벤트 허브 콘텐츠

### 7.2 비로그인 화면 (공개 랜딩 콘텐츠)

코드 현황(`src/app/page.tsx` → `Hub.tsx` 분기)에 따른 실제 레이아웃은 다음과 같습니다.

```
┌──────────────────────────────────────────┐
│  CampaignHero                            │
│  · 캠페인 제목 + 기간 (06.08 ─ 07.07)     │
│  · 🔴 LIVE strip — 실시간 슬롯 잔여       │
│  · CTA: [Sign up / Log in to join]       │
│  · ⓘ Open to YouthMeta members only ·    │
│      [Learn how to join] (외부 link-out)   │
├──────────────────────────────────────────┤
│  (dimmed preview · opacity 0.42)         │
│                                          │
│  ProgressTracker (3 단계 시각화)         │
│   · STEP 1: Sign up + connect OKX        │
│   · STEP 2: Trade $500 → 20 USDT         │
│   · STEP 3: Complete survey → ICX        │
│                                          │
│  UsdtRewardCard · IcxRewardCard          │
│   (보상 카드 흐림 프리뷰)                 │
└──────────────────────────────────────────┘
```

> 📌 **흐림 프리뷰**: Hub 본체(`ProgressTracker` + 보상 카드)를 `opacity 0.42 / saturate(0.85)` + `pointer-events-none`으로 비활성화 표시. `FrozenStateScope`로 mock state를 `initialState`로 고정해 디버그 토글이 프리뷰에 반영되지 않도록 함.

**비로그인 화면 정책**
- 개인화 영역(진척도 수치, 보상 상태)은 흐림 프리뷰로만 노출, 실제 데이터는 비공개
- 슬롯 잔여 카운터는 `CampaignHero` LIVE strip에서 노출 (FOMO 효과)
- 공유 가능한 URL — OG 태그 적용
- SEO 최적화 (검색 노출 허용)
- CTA는 `Sign up / Log in to join` 단일 — 클릭 시 **이벤트 페이지 내부에서 로그인하지 않고, 본 서비스 가입/로그인 페이지로 redirect**. 디바이스 판별에 따라 **모바일은 Supercycl Mobile PWA 로그인 페이지**, **PC는 Supercycl PC 로그인 페이지**로 분기 (각 본 서비스 버전의 자체 로그인 UI 사용) → 완료 후 동일 이벤트 URL로 복귀 (로그인 콘텐츠 자동 렌더링)
- **자격 고지** — Hero의 Sign in CTA 바로 아래에 `ⓘ Open to YouthMeta members only · Learn how to join` 라인을 inline subtle 텍스트로 노출. 메인 CTA 강조 약화 방지를 위해 카드/박스가 아닌 한 줄 텍스트. `Learn how to join` 텍스트만 외부 link-out(`NEXT_PUBLIC_YOUTHMETA_JOIN_URL`, `target="_blank"`). 비유스메타 유저의 무의미한 로그인 round-trip을 미리 줄이고, YouthMeta 가입 동선을 동시에 안내
- `HubHeader / SlotTension / MyProgressMeter / HubCtaBar` 등 로그인 전용 요소는 비로그인 화면에서 미노출 (코드 주석: "redundant or empty pre-auth")

### 7.3 로그인 화면 (이벤트 허브 콘텐츠)

```
┌──────────────────────────────────────┐
│  Supercycl Mobile Launch Festival    │
│  2026.06.08 ─ 07.07 · 1 MONTH        │
│  [로그인 상태]   [본 서비스로 →]      │
├──────────────────────────────────────┤
│  ✦ MY ACCOUNT                         │
│  Supercycl address  hx1234…5678       │
│  OKX UID            1234567890        │
│   (OKX 미연동 시: [Not connected] chip)  │
├──────────────────────────────────────┤
│  🎯 ProgressTracker (3단계)            │
│    1. Sign up + connect OKX            │
│       (OKX 미연동 시 라벨 아래             │
│        "OKX not connected" 서브라인 노출) │
│  ✅ STEP 1: Sign up + connect OKX [DONE]│
│  ⏳ STEP 2: Trade $500 → 20 USDT       │
│  ⬜ STEP 3: Complete survey → ICX      │
├──────────────────────────────────────┤
│  📊 실시간 슬롯                        │
│  거래 슬롯 잔여        423 / 500       │
├──────────────────────────────────────┤
│  💰 내가 받을 보상                     │
│                                       │
│  20 USDT — 거래 $500 달성 시          │
│   상태: 자격 충족 · AWAITING_PAYOUT    │
│   OKX UID: 12******78 (자동 송금 예정)│
│                                       │
│  ICX 보상 — 설문 완료 시                │
│   (거래자 100 ICX / 비거래자 ⏳)        │
│   상태: AWAITING_PAYOUT                │
│   OKX UID: 12******78 (자동 송금 예정)│
├──────────────────────────────────────┤
│  [거래하러 가기 →]   [설문 시작]       │
│   (본 서비스로 이동)                    │
└──────────────────────────────────────┘
```

#### ProgressTracker 3단계 (코드: `src/components/hub/ProgressTracker.tsx`)

| Step | 라벨 (en.ts) | done 조건 |
|---|---|---|
| 1 | `Sign up + connect OKX` | `state.hasOkxLinked` |
| 2 | `Trade $500 → 20 USDT` | Step 1 done **AND** `state.tradingVolume >= 500` |
| 3 | `Complete survey → ICX` | Step 2 done **AND** `state.surveyCompleted` |

상태 enum: `done | inProgress | locked`. **Strict sequential gating** — 각 단계의 `done`은 이전 단계가 `done`이어야만 진입 가능 (디버그 토글로 Step 2만 끄고 Step 3 done이 되는 케이스 방지). 이전 단계 미완 시 후속 단계는 `locked`.

#### 보상 영역 상태별 표시

내부 enum 키는 §8.3을 따르며, UX 라벨은 영문 단일 UI(§7.5 다국어 정책)로 다음과 같이 표시합니다.

| Enum 키 (§8.2) | USDT 영역 표시 | ICX 영역 표시 |
|------|----------|---------|
| `NOT_REACHED` | "Trading in progress ($X / $500)" + 영역 비활성 | "Available after survey completion" + 영역 비활성 |
| `AWAITING_PAYOUT` | "Awaiting payout · OKX UID 12****78" | "Awaiting payout · OKX UID 12****78" |
| `PENDING_PAYOUT` | "Pending payout · OKX UID 12****78" | "Pending payout · OKX UID 12****78" |
| `ON_HOLD` | "Under review (max 7 days)" | "Under review (max 7 days)" |
| `PAID` | "✓ Paid (YYYY-MM-DD · TX hash)" | "✓ Paid (YYYY-MM-DD · TX hash)" |
| `CAP_FULL` (USDT 전용) | "$500 reached — slot capacity full. Thank you." | — |

> UX 라벨은 동일 enum 값을 유저에게 친숙하게 풀어쓴 영문 표현입니다. 운영자/개발자는 enum 키(`NOT_REACHED / AWAITING_PAYOUT / PENDING_PAYOUT / ON_HOLD / PAID / CAP_FULL`)로 통일해서 소통합니다.

### 7.3.1 YouthMeta 미가입 상태 — 차단 화면

비유스메타 계정으로 본 서비스 로그인을 완료한 유저가 이벤트 페이지로 돌아왔을 때 노출되는 전용 화면입니다. **CampaignHero는 그대로 유지**해 어느 이벤트인지 컨텍스트를 보존하고, 본문은 차단 안내 + 두 개의 CTA만 표시합니다.

```
┌──────────────────────────────────────────┐
│  CampaignHero (그대로 노출)               │
│   · EVENT · LIVE · N days left           │
│   · Supercycl Mobile Launch Festival      │
│   · 2026.06.08 ─ 07.07                    │
├──────────────────────────────────────────┤
│                  🔒                       │
│        YouthMeta members only             │
│  This festival is exclusive to YouthMeta │
│  members. You're signed in, but your      │
│  account isn't on the YouthMeta roster.   │
│                                           │
│  [Become a YouthMeta member →]            │
│  [Sign out]                                │
└──────────────────────────────────────────┘
```

**활성화 조건**
- `authStatus === 'logged_in' AND isYouthMetaMember === false` (코드: `isBlockedNonYouthMeta` selector, `src/lib/mock-state/selectors.ts`)
- 본 서비스 로그인 직후 백엔드가 YouthMeta 멤버십을 확인해 이벤트 페이지에 전달 (§7.5 외부 페이지 연동 사양 참조)

**노출/숨김 규칙**
- 노출: `CampaignHero` + `YouthMetaGate` (제목/설명/잠금 아이콘/CTA 두 개)
- 숨김: `MyAccountCard`, `ProgressTracker`, `MyProgressMeter`, `UsdtRewardCard`, `IcxRewardCard`, `HubCtaBar`, `SurveyModal` 등 정상 허브의 모든 본문/모달
- 인앱 알림 트리거(§9.1)도 차단 화면에서는 발화 불가 (보상/슬롯 상태에 의존하므로)

**CTA 동작**
- `Become a YouthMeta member` — 외부 link-out (`NEXT_PUBLIC_YOUTHMETA_JOIN_URL`, `target="_blank" rel="noopener noreferrer"`). 이벤트 페이지 내부 가입 폼은 두지 않음 (가입 동선은 본 서비스/YouthMeta 측 자체 페이지에서 처리)
- `Sign out` — 본 서비스 세션 종료 후 동일 이벤트 URL로 복귀 → 공개 랜딩 콘텐츠 자동 렌더링. 실 구현에서는 본 서비스 로그아웃 엔드포인트 호출 필요 (구체 방식은 본 서비스 개발팀 협의 — §7.5)

**다국어 정책**: 차단 화면도 영어 단일 UI를 유지합니다 (§7.5).

### 7.4 설계 원칙

1. **단일 URL 진입점** — 로그인 여부에 따라 콘텐츠만 분기 (공개 랜딩 ↔ 이벤트 허브)
2. **3섹션 구조** (로그인) — 진척도 / 슬롯 / 보상
3. **My account 상단 노출** — 로그인 즉시 Supercycl 계정 주소 + 연동된 OKX UID를 Hero 바로 아래 read-only 카드로 노출 (지급 대상 식별·자가확인 가능). OKX 미연동 시: My account UID 자리에 `Not connected` chip, ProgressTracker step 1 라벨 아래에 `OKX not connected` 서브라인을 노출해 어떤 sub-action이 막혔는지 명시. **연동된 경우는 별도 "Connected" 표기 없이** Done 배지와 UID 값 자체로 신호 (중복 표기 제거).
4. **실시간 슬롯** — 5분 갱신, 100/50/10 도달 시 페이지 상단 배너 표시 (로그인/비로그인 공통)
5. **개인화** — 로그인 시 진행 단계에 따라 CTA 자동 전환
6. **보상 상태 표시** — `NOT_REACHED / AWAITING_PAYOUT / PENDING_PAYOUT / ON_HOLD / PAID / CAP_FULL` (§8.2 enum/UX 라벨 매핑 참조)
7. **전환 동선** — 가입 → 거래 → 설문 CTA로 sequential 흐름
8. **본 서비스 연계** — 거래/KYC CTA만 본 서비스 deeplink로 이동 (보상 지급은 운영자가 OKX Internal Transfer로 자동 처리)

### 7.5 외부 페이지 — 본 서비스 연동 사양

> 📌 **본 서비스 인프라 결정은 본 명세 범위 외**: 구체적 도메인, SSO/JWT/세션 인증 방식, Deeplink 스킴 URL은 **본 서비스 개발팀과 별도 협의**로 결정. 본 표는 인터페이스 요구사항(필요한 동작)만 정의.

| 항목 | 요구사항 |
|------|---------|
| **인증** | 이벤트 페이지에서 본 서비스 로그인 상태를 인식할 수 있어야 함 (방식은 본 서비스 개발팀 결정 — SSO/JWT/세션 등). **이벤트 페이지에서 별도 가입/로그인 불가** — 로그인 CTA 클릭 시 디바이스에 따라 Supercycl Mobile PWA 로그인 페이지(모바일) 또는 Supercycl PC 로그인 페이지(PC)로 redirect, 본 서비스 자체 로그인 UI에서 처리 |
| **YouthMeta 멤버십 조회** | 본 서비스 로그인 시 백엔드가 **YouthMeta 로스터 등록 여부(boolean)** 를 세션/공통 API에서 노출. 이벤트 페이지는 이 값을 read-only로 읽어 차단 화면(§7.3.1) 분기. 멤버십 동기화 지연으로 인한 false-negative는 CS 클레임 J(§10.1)로 처리 |
| **API 도메인** | 본 서비스와 동일 백엔드 API 호출 (CORS 설정 필요 — 본 서비스 개발팀 협의) |
| **Deeplink 스킴** | 본 서비스 거래 화면 / KYC 화면 / **로그인·가입 페이지(모바일 PWA용 / PC 웹용 각각)** / **YouthMeta 가입 페이지(외부)** deeplink/외부 URL은 본 서비스 개발팀과 협의 후 환경 설정에 주입 (예: `NEXT_PUBLIC_TRADE_URL`, `NEXT_PUBLIC_LOGIN_URL_MOBILE`, `NEXT_PUBLIC_LOGIN_URL_PC`, `NEXT_PUBLIC_YOUTHMETA_JOIN_URL`) |
| **Return URL** | 본 서비스 가입/OAuth 완료 후 이벤트 페이지로 자동 복귀 → 로그인 상태로 이벤트 허브 콘텐츠 자동 렌더링 (구체 URL은 본 서비스 개발팀 협의) |
| **세션 동기화** | 본 서비스에서 KYC/거래 완료 시 이벤트 페이지 자동 갱신 (실시간 또는 새로고침) |
| **반응형** | 모바일/PC 모두 대응 (이벤트 페이지 단일 코드베이스) |
| **다국어** | **영어 단일 UI 운영** — 모든 UI 텍스트, 상태 라벨, 안내 메시지는 영어로만 운영. 한국어는 **설문 12문항 콘텐츠**(`src/content/survey-ko.md`)에만 한정. 언어 전환 토글 미제공. 라이브러리는 **`next-intl`**을 사용하여 locale 분리 구조는 유지(향후 한국어 UI 확장 시 키 매핑만 추가하면 되도록 대비). |
| **SEO/공유** | 단일 URL 운영 — 비로그인 진입 시 공개 랜딩 콘텐츠 자동 노출. OG 태그, 검색 노출 허용 |

---

## 8. 보상 지급 일정

| 보상 | 주기 | 채널 |
|------|------|------|
| ICX (설문) — 거래자 100 / 비거래자 ⏳ | 완료 후 7영업일 이내 | OKX Internal Transfer (연동된 OKX UID) |
| 20 USDT (거래) | 매주 월 10:00 KST | OKX Internal Transfer (연동된 OKX UID) |

> 이벤트 페이지 보상 영역에서 "지급 대기 / 완료" 상태 동기화 표시

### 8.1 수동 지급 운영 플로우 ⚠️

**모든 보상은 운영팀이 수동으로 송금합니다. 유저 측 클레임 버튼은 없습니다.**

```
[유저 측]
설문 완료 또는 거래 자격 충족
        ↓
시스템이 자동으로 AWAITING_PAYOUT 상태 생성
        ↓
유저: 이벤트 페이지 보상 영역에서 상태 확인만 수행 (별도 입력 없음)

[운영자 측]
운영자 도구(어드민 시스템 외부 — §0.0 참조)에서 자격 충족자 리스트 확인
        ↓
대상자 리스트 export (CSV 등 — 연동된 OKX UID 포함)
        ↓
운영자가 OKX Internal Transfer로 송금 실행
        ↓
백엔드 API로 유저별 "지급 완료" 상태 마킹 (송금 TX 해시 기록 가능)
        ↓
시스템: 이벤트 페이지 보상 상태 라벨 즉시 자동 갱신 (`AWAITING_PAYOUT` → `PAID`)
```

### 8.2 이벤트 페이지 — 보상 상태 표시

#### 내부 상태값 ↔ UX 라벨 분리 원칙

상태값은 **영문 enum**으로 정의해 백엔드 API / DB / 프론트 코드 전반에서 공통 사용하고, 유저에게 보이는 라벨은 **UX 라벨(영문 단일 UI)** 로 별도 정의합니다. enum 키와 UX 라벨이 1:N (USDT/ICX 컨텍스트에 따라 라벨 다르게 표현 가능)으로 매핑됩니다.

#### 상태 enum & UX 라벨 매핑 (USDT/ICX 공통, USDT 전용 1종 별도)

| Enum 키 | 의미 | USDT UX 라벨 | ICX UX 라벨 |
|---|---|---|---|
| `NOT_REACHED` | 자격 미충족 (거래 진행 중 / 설문 미응답) | "Locked" | "Locked" |
| `AWAITING_PAYOUT` | 자격 충족, **운영자 송금 대기** (유저 입력 단계 없음 — 연동된 OKX UID로 자동 송금 예정) | "Awaiting payout" | "Awaiting payout" |
| `PENDING_PAYOUT` | 운영자가 송금 큐에 잡았으나 아직 실행 전 | "Pending payout" | "Pending payout" |
| `ON_HOLD` | 의심 케이스로 검토 중 (최대 7일) | "Under review (max 7 days)" | "Under review (max 7 days)" |
| `PAID` | 송금 완료 (완료일자 표시, TX 해시 있으면 링크) | "Paid" | "Paid" |
| `CAP_FULL` (USDT 전용) | 슬롯 마감 후 $500 도달 — 보상 지급 불가 | "$500 reached — slot capacity full. Thank you." | — |

> 📌 **유저 액션**: 보상 영역은 read-only 상태 표시입니다. 등록/클레임 버튼은 존재하지 않으며, 연동된 OKX UID(마스킹 처리)와 현재 상태 라벨만 노출합니다.

---

## 9. 인앱 알림 정책

> 이메일 / 푸시 알림은 사용하지 않습니다. 모든 사용자 안내는 **이벤트 페이지 인앱 표시**(토스트, 배너, 상태 라벨, 인앱 메시지 카드)로만 처리합니다.

### 9.1 인앱 표시 트리거

| # | 시점 | 표시 방식 | 메시지 |
|---|------|---------|--------|
| 1 | 이벤트 페이지 첫 진입 | 상단 배너 | "Supercycl Mobile Launch Festival 진행 중 (D-day ~ 종료일)" |
| 2 | 설문 완료 직후 (= ICX 자격 충족 시점) | 모달 | 미니 리포트 + **연동된 OKX UID로 자동 송금 예정 안내** (Done 버튼만 노출, 별도 입력 단계 없음) |
| 3 | 슬롯 잔여 100/50/10 | 슬롯 카운터 색상 변경 + 페이지 상단 배너 | "잔여 슬롯 X개" |
| 4 | 운영자 "지급 완료" 마킹 시 | 보상 상태 라벨 `PAID`로 자동 갱신 + 토스트 (다음 페이지 방문 시) | "USDT/ICX 지급이 완료되었습니다" |
| 5 | 종료 D-3 | 페이지 상단 배너 | "이벤트 마감 3일 전" |

> 📌 **이전 9개 트리거에서 변경된 점**: 환영 카드(#2), 진척도 50% 메시지 카드(#4), USDT $500 도달 모달(#6), 종료 직후 NPS 모달(#9) 4건 제거. 트리거 5개로 축소.

### 9.2 인앱 표시 설계 원칙

- **외부 발송 채널 없음** — 이메일, SMS, 푸시(웹/앱) 모두 사용 안 함
- **상태 기반 표시** — 유저가 페이지를 열었을 때 현재 상태를 보여줌 (서버 polling 또는 페이지 진입 시 fetch)
- **읽음 처리** — 모달/토스트는 1회 표시 후 dismiss 상태 저장 (서버 측 user_flag). 현재 정의된 키:
  - `surveyCompleteSeen` — 설문 완료 직후 미니 리포트 모달(§9.1 #2)을 1회 표시 후 dismiss
  - 향후 트리거 추가 시 동일 패턴(`<trigger>Seen`) 키 확장
- **배너/카드 우선순위** — 동시 발생 시 우선순위: 자격 충족 > 보상 지급 완료 > 진척도 마일스톤 > 일반 안내
- **외부 채널 전환 시 영향** — 향후 알림 채널이 필요해질 경우 §9.1 트리거 그대로 외부 채널에 매핑 가능 (확장 여지 유지)

---

## 10. CS 운영 정책 (SOP)

이벤트 운영 중 발생 가능한 주요 클레임 시나리오에 대한 처리 기준입니다. 상세 응답 스크립트는 별도 CS 매뉴얼에서 관리합니다.

### 10.1 클레임 카테고리별 SOP

| # | 클레임 시나리오 | 1차 응대 | 내부 처리 절차 | 처리 SLA |
|---|----------------|---------|--------------|---------|
| **A** | "거래했는데 진척도에 안 잡힘" | 거래 시각 확인 → 자격 도달 처리 사이클 이후 반영 안내 (백엔드 처리 주기 안내) | 다음 영업일 진척도 미반영 시 운영자 도구에서 브로커 API 로그 조회 → 자전거래/미체결 등 거래량 미인정 사유 확인 → 사유 회신 또는 수동 보정 | 영업일 1일 |
| **B** | "지급 완료라고 떴는데 못 받음" | 운영자 도구에서 TX 해시 + 연동된 OKX UID 확인 → 유저에게 공유 | 1) OKX 연동 UID와 실제 OKX 계정 동일 여부 확인 2) TX 해시 미존재 시 운영자 송금 누락 확인 → 재송금 | 영업일 2일 |
| **C** | "OKX 연동 계정과 송금 받을 계정이 다르다" | OAuth 재연동 안내 (본 서비스에서 OKX 재연결) | 미지급 상태(`AWAITING_PAYOUT` / `PENDING_PAYOUT`)면 OAuth 갱신 후 새 UID로 송금. 이미 `PAID`면 회수 불가 안내 (1차 응대 시 명시) | 영업일 1일 |
| **E** | "슬롯에 들어갔는데 떨어졌다" | 운영자 도구에서 슬롯 박탈 여부 확인 → 사유(어뷰징/보류 등) 회신 | 박탈 사유는 운영팀 어뷰징/의심 케이스 정책에 따른 판단 결과로 회신 (운영 매뉴얼) | 영업일 2일 |
| **F** | "설문 응답이 저장 안 됨" | 재응답 안내 (1인 1회 제약은 서버측 검증이므로 재응답 시 중복 차단) | 서버 로그에서 응답 저장 시점 확인 → 미저장 시 운영자가 수동으로 응답 등록 | 영업일 1일 |
| **H** | "거래소 OAuth 연결이 안 됨" | 본 서비스 CS로 이관 (이벤트 외 본 서비스 기능) | — | 본 서비스 CS SLA |
| **I** | "이벤트 페이지에 로그인이 안 됨" | 본 서비스 로그인 상태 확인 안내 → 본 서비스 로그인 후 이벤트 페이지 재진입 | SSO/토큰 동기화 이슈인 경우 개발팀 이슈 트래커 등록 | 영업일 1일 |
| **J** | "로그인했는데 YouthMeta-only 차단 화면이 떴다" | YouthMeta 멤버십 등록 여부 확인 → 미등록자에게는 YouthMeta 가입 안내(§7.3.1 link-out 동일) → 본인이 YouthMeta 멤버라고 주장하면 본 서비스 로스터 동기화 확인 절차 안내 | YouthMeta 로스터에 등록되어 있는데도 false-negative로 차단된 케이스는 본 서비스 멤버십 동기화 이슈로 개발팀 이슈 트래커 등록 | 영업일 1일 |

### 10.2 클레임 응대 원칙

- **확인 가능한 사실 우선** — TX 해시, 연동된 OKX UID, 브로커 로그 등 운영자 도구로 확인 가능한 데이터 기반으로 회신
- **운영팀 의사결정 사항은 명확히 분리** — 정책상 자동 처리 vs 수동 판단 분기를 1차 응대에서 명시
- **운영 매뉴얼 참조** — 슬롯 박탈, 어뷰징 보류 등은 운영팀 매뉴얼 조항 인용 (별도 이벤트 약관 없음)
- **본 서비스 이슈는 즉시 이관** — KYC, 거래소 연결, 거래 실행 관련은 본 서비스 CS로 핸드오프

---

## 11. 제품 검증 트랙 (Live Validation Layer)

캠페인 운영 자체에 **4개 검증 영역**을 임베드. 모든 데이터는 종료 후 별도 인사이트 리포트로 정리.

> 📌 **추적 도구·구현 책임**: 본 섹션은 측정 영역과 핵심 가설만 정의합니다. 구체적 **추적 도구 선정**(Mixpanel/Amplitude/Segment/자체 로깅 등), **이벤트 스키마 설계**, **본 서비스/이벤트 페이지 책임 분담**은 **데이터팀·제품팀이 자체 결정**합니다 (본 명세 범위 외).

### 11.1 Onboarding 퍼널 이탈 측정 (전 기간)

| Step | 단계 | 측정 |
|------|------|------|
| 1 | 이벤트 랜딩 진입 | 100% (기준점) |
| 2 | 회원가입 시작 | ?% |
| **3 (Hypothesis)** | **거래소 연결** | **?%** |
| 4 | 첫 거래 체결 | ?% |

> **핵심 가설:** "거래소 연결" 단계에서 50% 이상 이탈 시 → 연결 UX 재설계 필요

### 11.2 First Trade Time-to-Conversion
- 가입 ~ 첫 거래까지 시간 분포 (24h / 72h / 7d 도달률)
- 가설: 가입 후 24h 내 미거래 시 90% 이탈
- 병행 측정: $500 도달까지 평균 거래 횟수

### 11.3 ⭐ 시그널 개인화 효과 검증 (CORE)

| 측정 항목 | 의미 |
|----------|------|
| 시그널 인앱 노출 → 클릭률 (CTR, 본 서비스 PWA 내) | 개인화 추천이 의미 있는가 |
| 시그널 → 주문 실행률 | 추천이 실제 거래로 이어지는가 |
| 시그널 vs 자체 판단 — 승률/PnL | 시그널의 객관적 가치 입증 |

**산출물:** "비슷한 성향 유저들의 평균 월 수익률 X%" — 미니 리포트 및 차기 캠페인 카피 데이터셋

### 11.4 ⭐⭐ 2x 레버리지 캡 이탈 검증 (TOP PRIORITY)

YouthMeta 정책의 핵심 의사결정에 직결되는 단일 최우선 인사이트.

| # | 측정 항목 | 측정 방법 |
|---|----------|----------|
| A | 2x 노티스 모달 노출 후 이탈률 | SCR-102 진입 후 모달 노출 → 다음 액션까지 측정 |
| B | 레버리지 시도 패턴 | 슬라이더 드래그 로그 — 2x 위로 올리려다 막힌 횟수 |
| C | 첫 거래 vs 미거래 유저 비교 | 노티스 본 유저 중 7일 내 첫 거래 도달률 |
| D | **별도 설문 / 1:1 인터뷰** | 가입 후 14일간 거래 0건 유저 표본 대상 — 별도 폼 또는 1:1 인터뷰 진행 (§4.3 정식 설문과 별개) |

**산출 인사이트:** "YouthMeta 멤버 중 X%가 2x 캡 때문에 거래 미체결" → 정책 유지/완화/세분화 의사결정 근거

### 11.5 검증 트랙 산출물

| # | 산출물 | 용도 |
|---|--------|------|
| 1 | 퍼널 이탈 리포트 | 단계별 drop-off 시각화 |
| 2 | 설문 결과 인사이트 문서 | 제품 로드맵 권고 (우선순위 매핑) |
| 3 | 시그널 가치 입증 데이터셋 | 마케팅팀 활용 (차기 캠페인 카피) |
| 4 | **2x 레버리지 정책 영향 분석** | 경영진 배포 (정책 의사결정 직결) |

---

## 12. Open Issues — 착수 전 확정 필요

| # | 이슈 | 내용 |
|---|------|------|
| 1 | 운영 매뉴얼 작성 | 슬롯 박탈/어뷰징 보류/분쟁 처리 등 운영 재량 항목을 정리한 운영팀 내부 매뉴얼 (별도 이벤트 약관 없음 — §0.0 참조) |
| 2 | YouthMeta 커뮤니티 채널 존재 여부 | 디스코드/텔레그램 부재 시 Tier 2 전략 조정 |
| 3 | 슬롯 조기 소진 시 확장 결정자 | +200슬롯 확장 권한 결정자 지정 |
| 4 | ICX/USDT OKX Internal Transfer 운영 책임자 지정 | 자격 충족자 리스트 확인 + OKX Internal Transfer 실행 담당자, 권한 분리 여부 |
| **12** | **비거래자 ICX 인당 지급량 확정** | **§3 풀 분리 정책 — 30,000 ICX를 몇 명에게 인당 얼마씩 지급할지 (예: 600명 × 50 ICX, 또는 300명 × 100 ICX 등) 확정 필요** |
| **6** | **개발 일정 정합성** | **D-day(2026-06-08) vs 개발 리드타임 — 이벤트 페이지 / 슬롯 카운터 / 진척도 바 완료 시점 점검** |
| **7** | **이벤트 페이지 archive 정책** | **캠페인 종료 후 페이지 유지 기간, archive 모드 전환 방식** |

> 📌 **본 서비스 인프라/인증/배포 관련 결정** (이벤트 페이지 도메인, SSO/JWT 인증 공유 방식, 본 서비스 Deeplink 스킴 등)은 **본 서비스 개발팀과 별도 협의**로 처리하며 본 명세 범위 외입니다. §7.5 외부 페이지 연동 사양은 인터페이스 요구사항만 정의합니다.

---

## 13. 개발 우선순위 체크리스트

### 🔴 P0 — 필수 (D-day 전 반드시 완료)

**[이벤트 페이지 — 외부]**
- [ ] **이벤트 페이지 인프라 셋업** (도메인, SSL, 배포 파이프라인)
- [ ] **본 서비스와 인증 공유** (SSO/JWT/세션)
- [ ] **공통 API CORS 설정**
- [ ] 이벤트 허브 화면 (로그인 시: 진척도 + 슬롯 + 보상)
- [ ] **🆕 공개 랜딩 콘텐츠 (비로그인 시 동일 URL에서 자동 렌더링) — OG 태그, SEO, CampaignHero LIVE 슬롯 카운터, `Sign up / Log in to join` 단일 CTA, 흐림 프리뷰 (§7.2 참조)**
- [ ] **🆕 YouthMeta 멤버십 게이팅** — 공개 랜딩에 자격 고지 라인(§7.2) + 비유스메타 로그인 유저 차단 화면(§7.3.1, `YouthMetaGate`) + 외부 가입 link-out(`NEXT_PUBLIC_YOUTHMETA_JOIN_URL`)
- [ ] **🆕 i18n 구조 셋업 (`next-intl`)** — UI 텍스트는 **영어 단일** 운영(상태 라벨, 안내 메시지, CTA 포함). 한국어는 **설문 12문항 콘텐츠 한정**(`src/content/survey-ko.md`). 언어 전환 토글 미제공. locale 분리 구조는 유지하여 향후 한국어 UI 확장 대비
- [ ] 설문 12문항 폼 + 응답 저장 (`src/content/survey-ko.md` 참조)
- [ ] 슬롯 카운터 (5분 갱신)
- [ ] 진척도 바 (실시간 거래량 표시)
- [ ] 보상 상태 표시 (`NOT_REACHED/AWAITING_PAYOUT/PENDING_PAYOUT/ON_HOLD/PAID/CAP_FULL` — §8.2 매핑 참조)
- [ ] **연동된 OKX UID 마스킹 노출** (자격 충족 시 보상 카드에 read-only로 표시 — 별도 입력 모달 없음)
- [ ] **본 서비스 Deeplink 연결** (거래/KYC)
- [ ] **Return URL 처리** (본 서비스 → 이벤트 페이지 복귀)

**[본 서비스 — 기존 Mobile/PC]**
- [ ] 가입 → KYC → 거래소 OAuth 통합 플로우 (Return URL 지원, 완료 시 이벤트 페이지로 자동 redirect)
- [ ] OAuth 완료 시 OKX UID를 세션/공통 API에서 노출 (이벤트 페이지가 read-only로 읽을 수 있도록)
- [ ] **🆕 로그인 응답에 YouthMeta 멤버 플래그(boolean) 노출** — 이벤트 페이지가 차단 화면 분기에 사용 (§7.5)
- [ ] ~~이벤트 페이지로 돌아가기 링크/버튼~~ → **별도 동선 미제공** (브라우저 뒤로가기/탭 전환으로 복귀)

**[백엔드 — 공통]**
- [ ] 자격 검증 백엔드 구현 (배치/실시간/이벤트 기반 등 방식은 백엔드 자체 판단, §5.5 참조) — 단, 주간 정산 D-1(일요일 23:59 KST) 이전 거래는 모두 다음 정산일 전에 반영 필요
- [ ] 어뷰징 방지 로직 (디바이스 fingerprint, 자전거래 탐지)
- [ ] **지급 큐 / TX 해시 / 지급 완료 마킹 API** (수령 정보 입력 기능 없음 — 연동된 OKX UID를 destination으로 사용)

### 🟡 P1 — 운영 필수
- [ ] 5개 인앱 표시 트리거 구현 (배너/모달/토스트/상태 라벨 — §9.1 참조)
- [ ] **"지급 완료" 토글 시 보상 상태 라벨 자동 갱신 (인앱)**
- [ ] CS FAQ 페이지 (이벤트 페이지 내) — 20문항+, SOP는 §10 참조
- [ ] 2x 레버리지 이벤트 추적 (본 서비스, 모달 노출/슬라이더 드래그 로그)
- [ ] **이벤트 페이지 ↔ 본 서비스 세션 동기화** (KYC/거래 완료 시 실시간 반영)

### 🟢 P2 — 검증/리포팅
- [ ] 퍼널 단계별 이벤트 추적 (4단계)
- [ ] First Trade Time-to-Conversion 측정
- [ ] 시그널 CTR / 주문 실행률 / PnL 비교 로깅
- [ ] 종료 후 인사이트 리포트 데이터 추출 쿼리

---

## 14. 주요 일정 요약

| 날짜 | 이벤트 |
|------|--------|
| 2026-06-08 (월) 00:00 | **캠페인 시작 (D-day)** |
| 2026-06-15 (월) 10:00 | 1차 주간 정산 (W1 거래: 6/8~6/14) |
| 2026-06-22 (월) 10:00 | 2차 주간 정산 (W2 거래: 6/15~6/21) |
| 2026-06-28 (일) 23:59 | **거래 이벤트 종료** |
| 2026-06-29 (월) 10:00 | 3차 주간 정산 (W3 거래: 6/22~6/28) + **설문 이벤트 시작** |
| 2026-07-05 (일) 23:59 | **설문 이벤트 종료** |
| 2026-07-06 (월) 10:00 | 4차(최종) 주간 정산 |
| 2026-07-07 (화) 23:59 | **캠페인 종료** |
